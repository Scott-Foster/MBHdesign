".onLoad" <- function( libname, pkgname){
#  packageStartupMessage("This is MBHdesign version 2.1.2")
#  packageStartupMessage("It is for generating spatially balanced designs for points and transects (possibly with legacy)")
}
